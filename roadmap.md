# A roadmap to large language model training

## Data

need pretraining data, collect from website, many format problem (pdf, webpage, etc.)

Data sourcing, collection, cleaning, deduplication, filtering, and mixture design -> large scale scrapling

now webdate is completely used, must synthetic data. problem -> answer by AI, check. autogenerated problem, (theorem, what is ??(related concepts))

synthetic data -> agent, prompting, templates

integrate with RL

chain of thought data

## Pre-training strategy

data mixture, phase of training, stability

architecture, training, parameter tuning, Continued pretraining or domain adaptation -> llm basic llm.md

## Supervised finetuning strategy

SFT -> data, agent

## Reinforcement learning strategy

RL -> 

evaluation -> domain specific

## Multi-modal model

## Large scale training infra

operator acceleration level

distributed level: distributed training (machine, megatron) -> llm infra, pipeline, data, tensor, expert

RL, training inference alignment, rollout, async

Inference-time optimization, serving assumptions, quantization, and deployment constraints

## Task and benchmark

searching, tool-use (agent), math, coding, ...

## Specific problems for math llm

MATH LLM, searching, synthetic data